* Run unit tests
  `python -m unittest -v test_employee.py`
* Run integration tests
  `python -m unittest -v test_integration.py`
* Run all tests (regression / discovery)
  `python -m unittest discover -v`
* Run code
  `python main.py`

